## What's New

- Initial release of MCPJungle for Cloudron
- Enterprise mode with Bearer token authentication
- PostgreSQL addon for persistent configuration
- Local storage addon for credentials and state
- Pre-installed runtimes: Node.js 20, Python 3, uv/uvx
- Register any MCP server (stdio or streamable-http)
- Centralized HTTPS endpoint for all MCP clients
